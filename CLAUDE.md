# payments-calculator — חוקת הפרויקט

מחשבון תוספת תשלומים לעובדי Triple Y. עמוד HTML יחיד, בלי שרת ובלי תלויות.
ההסבר המלא: [README.md](README.md). הפתוחים: [TASKS.md](TASKS.md).

## 🔴 הטבלה יושבת בשני מקומות, ויש שער שתופס סחיפה

`data/fee-table.json` הוא מקור האמת, ובלוק `<script id="fee-table">` שבתוך
`index.html` הוא עותק. העותק קיים כדי שהדף יעבוד בפתיחה ישירה מהדיסק, בלי
שרת ובלי `fetch`.

⟹ **שינוי אחוזים נעשה בשני המקומות.** ‏`node test.mjs` מפיל את הריצה אם הם
נפרדו. בלי השער הזה העדכון החלקי היה נראה מוצלח לגמרי, והמוכר היה קורא
ללקוח מספר ישן.

## 🔴 החישוב בשלמים, ולא בנקודה צפה

הסכום מומר לאגורות והאחוז שמור כמאיות האחוז (`2250` = 2.250%).
`Math.ceil` על מכפלה בנקודה צפה שנופלת בדיוק על עשרת עלול לקפוץ עשרת שלמה
בגלל שארית של 1e-15. **אין שגיאה ואין רמז**, והלקוח מחויב ב-10 ש"ח מיותרים.

📌 **נמדד ולא הונח: 10.5 מיליון צירופים, אפס פערים בין שתי השיטות היום.**
השלמים נשארים בכל זאת, כי הם מוציאים את השאלה מהתמונה לתמיד.

## 🔴 העיגול הוא כלפי מעלה, גם על שליש אגורה

`150` ש"ח ב-24 תשלומים נותן מכפלה של `10.0035`, והתוספת יוצאת **20** ולא 10.
זה מה שהכלל אומר, וזה גם מה ש-`ROUNDUP` באקסל מחזיר.
⚠️ **לא "לתקן" את זה בלי הכרעה של יובל.** רשום ב-TASKS.md.

## 🔴 התוספת היא המספר הגדול בדף, ולא הסך הכול

**הכרעת יובל 06.09.2026:** *"זה מה שחשוב בסוף, תכלס זה מה שהם צריכים
להגיד ללקוח."* ⟹ התוספת יושבת בגוש שחור בראש הכרטיס, ב-56 פיקסל,
והסך הכול ירד ל-20. **אין להחזיר את ההיררכיה ההפוכה.**
⚠️ האחוז ליד התוספת הוסר באותה בקשה. הוא נשאר בקוד ובנתונים.

## 🔴 העיצוב נמדד מהחנות, ולא נבחר בעין

רקע `#FFFFFF` · טקסט `rgb(18,18,18)` · גופן **Rubik** · פינות ישרות.
נשלף מ-`www.tripley.co.il` בדפדפן.
🔴 **Rubik עד משקל 700 בלבד.** מעל זה העברית נופלת לגופן מערכת והאנגלית
נשארת ב-Rubik. מתועד בהרחבה בחוקת `shopify-agent`.

**הדף מחויב למראה בהיר אחד ואין בו מצב כהה.** זו הכרעה ולא השמטה: הלוגו
נושא שחור, ועל רקע כהה הוא היה נעלם.

## 🔴 הלוגו מוטמע כ-data URI, ואינו מקושר

קישור אל ה-CDN של שופיפיי נחסם בכל הקשר שמגביל מקורות, ושובר את הדף
בפתיחה מהדיסק בלי אינטרנט. ההטמעה היא מה שמאפשר לדף להישאר **קובץ אחד
שעובד בכל מקום**.

`assets/` מוחרג מגיט (‏TY-HYG-001), ולכן **מקור הלוגו הוא החנות ולא הריפו**:
```bash
node embed-logo.mjs      # מושך מהחנות ומטמיע. אידמפוטנטי
```
יש בו שער שמפיל ריצה על PNG בלי ערוץ אלפא. לוגו עם רקע לבן אטום נראה
כמלבן על הדף, וזה מתגלה רק בעין.

## 🔴 השדות הם type="text" בכוונה

ב-`type="number"` הדפדפן בולע תו פסול ומחזיר מחרוזת ריקה. אז אי אפשר
להבחין בין "הוקלדה אות" לבין "השדה ריק", **ולכן אי אפשר להציג את הודעת
השגיאה הנכונה**. יובל ביקש הודעה מותאמת לכל הפרה, ולכן הוולידציה שלנו.

## 📌 התשלום החודשי בדף הוא חלוקה פשוטה

הפיצול המדויק, שבו השארית נגבית בתשלום הראשון, ירד מהתצוגה בבקשת יובל
‏06.09.2026 לטובת פשטות. **הוא עדיין קיים ב-`payments-fee.mjs`** בפונקציה
`quote`, עם שער שמוודא שהסכימה חוזרת בדיוק לסך הכול.

## אחרי כל שינוי

```bash
node test.mjs              # 23,278 תנאים
node test.mjs --negative   # מוודא שהשער בכלל מודד
```

**בקרה שלילית היא חלק מהמשימה.** בדיקה שלא הורצה מול מצב שבור אינה ידועה
כמודדת. שתי הבקרות כאן נכשלו כמצופה ב-06.09.2026.

## מה לא לעשות

- **לא להוסיף תלות ולא שלב בנייה.** הערך של הפרויקט הוא שהוא קובץ אחד
  שנפתח מכל מקום, גם בלי אינטרנט וגם מכונן משותף.
- **לא לנחש אחוז שאינו בטבלה.** מעל 24 תשלומים הדף חוסם, וזה נכון.
- **לא לכתוב מקף ארוך** בטקסט שהמוכר או הלקוח רואים.
